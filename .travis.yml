sudo: required

language: node_js

# services:
#   - docker

before_install:
# - docker pull node:alpine
# - docker run -d -p 80:3001 --expose 3001 -v $(pwd):/opt/app/ -w=/opt/app/  node:alpine /bin/sh -c "npm install && npm run dev"
# - docker ps -a
npm install

script:
- npm run dev
